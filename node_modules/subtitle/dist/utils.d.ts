import { Duplex, DuplexOptions } from 'readable-stream';
export declare const padLeft: (value: number, length?: number) => string;
export declare const createDuplex: (options: DuplexOptions) => Duplex;
